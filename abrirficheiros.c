//FILE *f = fopen("dados.txt", "r"); // "r" = leitura / "w"= escrita ( apaga se existir algo)"a"= acrescenta 
//fprintf(f, "Temperatura: %.1f\n", 23.5); escreve no ficheiro
//fclose(f); ( tenho de fechar o ficheiro sempre que termino)
// LER  FICHEIRO = FILE *f = fopen("dados.txt", "r"); // char linha[100]; //fgets(linha, sizeof(linha), f);
fclose(f);
#include <stdio.h>
